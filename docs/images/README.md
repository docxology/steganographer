# docs/images

Image assets referenced by `docs/` pages and the root README (e.g.
`dashboard-demo.webp`, `dashboard-video-tab.png`). Keep only assets actually
referenced from Markdown; remove unreferenced images.
