# AGENTS.md — `steganographer-dashboard/src/`

- `lib.rs`, `ws_handler.rs`, `static/` (verified 2026-08-29).
- `static/` assets are served verbatim; keep names stable — the HTML
  references them directly.
- Local-only — never commit.
