# src/

Dashboard crate source: `lib.rs` (Axum app assembly), `ws_handler.rs`
(WebSocket streaming), and `static/` (six web assets for the 3-tab GUI:
Video/Audio/Docs). Served by the `dashboard` CLI subcommand.
