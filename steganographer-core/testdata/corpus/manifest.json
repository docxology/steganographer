{
  "corpus_version": 1,
  "notes": [
    "FOR-001 calibration corpus (roadmap v0.8.0). Every entry maps a stable detector ID from",
    "steganographer_core::forensics::detector_registry() to a benign sample (must NOT trigger",
    "that detector) and a triggered sample (must trigger), with expected finding counts.",
    "Samples are small deterministic byte arrays generated by tests/calibration.rs from the",
    "generator specs below (no stored bytes, no hand-computed hashes); every fixture is",
    "<= 64 KiB.",
    "The 'balanced_ascii' generator repeats a hand-shuffled 26-letter alphabet whose byte",
    "value pairs are kept count-balanced so the statistical detectors stay quiet; this",
    "isolates each text/magic/container detector's behavior from the documented chi-squared",
    "false-positive limit on skewed buffers.",
    "STAT_RS pins a known limitation: its F1 and F-1 masks are identical in the current",
    "implementation (both map 2i <-> 2i+1), so the regular/singular difference metric is",
    "identically 0 and no sample can trigger it. This row must be updated when FOR-002",
    "revisits the masks."
  ],
  "detectors": [
    {
      "id": "STAT_CHI2",
      "benign": { "kind": "ascending_ramp", "reps": 128 },
      "triggered": { "kind": "even_ramp", "reps": 256 },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "Value-pair histogram balance: a full ascending ramp keeps (2i, 2i+1) pairs equal; an even-only ramp drives chi-squared above the critical value."
    },
    {
      "id": "STAT_SPA",
      "benign": { "kind": "even_ramp", "reps": 256 },
      "triggered": { "kind": "ascending_ramp", "reps": 128 },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "SPA fires on equalised value pairs with elevated close-pair ratio (adjacent ramp bytes differ by 1)."
    },
    {
      "id": "STAT_RS",
      "benign": { "kind": "ascending_ramp", "reps": 128 },
      "triggered": { "kind": "even_ramp", "reps": 256 },
      "expected": { "benign_findings": 0, "triggered_findings": 0 },
      "note": "Known limitation: identical flip masks make the difference metric identically 0 (see corpus notes)."
    },
    {
      "id": "MAGIC_FILE_FAMILY",
      "benign": { "kind": "balanced_ascii", "reps": 3 },
      "triggered": { "kind": "png_header" },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "Benign text has no known magic prefix (unknown family); the PNG header identifies the png family."
    },
    {
      "id": "MAGIC_EMBEDDED",
      "benign": { "kind": "balanced_ascii", "reps": 3 },
      "triggered": { "kind": "inline_packet_magic" },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "One inline STG3 packet magic in otherwise inert bytes."
    },
    {
      "id": "ZERO_WIDTH",
      "benign": { "kind": "balanced_ascii", "reps": 3 },
      "triggered": { "kind": "text_zero_width", "run_pairs": 6 },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "A 12-character ZWSP/ZWNJ run crosses the 8-character bit-encoding threshold."
    },
    {
      "id": "VARIATION_SELECTORS",
      "benign": { "kind": "balanced_ascii", "reps": 3 },
      "triggered": { "kind": "text_variation_selector" },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "A CDP variation selector (U+E0100) outside emoji context is always flagged."
    },
    {
      "id": "BIDI_CONTROLS",
      "benign": { "kind": "balanced_ascii", "reps": 3 },
      "triggered": { "kind": "text_bidi" },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "A single U+202E override is the trojan-source class marker."
    },
    {
      "id": "WHITESPACE_ANOMALY",
      "benign": { "kind": "balanced_ascii", "reps": 3 },
      "triggered": { "kind": "text_trailing_whitespace", "lines": 6, "run": 4 },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "Six lines with 4 trailing spaces each form the classic per-line whitespace channel."
    },
    {
      "id": "HOMOGLYPH_SUSPECT",
      "benign": { "kind": "balanced_ascii", "reps": 3 },
      "triggered": { "kind": "text_homoglyph" },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "Cyrillic a (U+0430) inside an ASCII word is a strong confusable."
    },
    {
      "id": "ZIP_TOPOLOGY",
      "benign": { "kind": "balanced_ascii", "reps": 3 },
      "triggered": { "kind": "zip_generic" },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "Every ZIP-family input yields exactly one inventory observation; non-ZIP input yields none."
    },
    {
      "id": "DOC-001",
      "benign": { "kind": "docx_clean" },
      "triggered": { "kind": "docx_oversized_claim" },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "The triggered docx claims ~2 GiB uncompressed for its main part, exceeding the 4 MiB per-entry inflate budget; the rejection is DOC-001 evidence and never sets the steganographic verdict."
    },
    {
      "id": "DOC-002",
      "benign": { "kind": "docx_clean" },
      "triggered": { "kind": "docx_laced", "run_pairs": 6 },
      "expected": { "benign_findings": 0, "triggered_findings": 1 },
      "note": "The triggered docx carries a zero-width-laced word/document.xml; the concealment finding sets the scan verdict."
    }
  ]
}