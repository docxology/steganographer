# benches/

Benchmarks for steganographer-core performance measurement. Not run by
`cargo test`; use `cargo bench` from the crate root (unverified — no bench
harness was executed during the doc pass).
