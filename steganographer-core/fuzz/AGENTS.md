# AGENTS.md — `steganographer-core/fuzz/`

- `fuzz_targets/` child (verified 2026-08-29); invocation not executed during
  the doc pass — unverified.
- Fuzz artifacts (crash/corpus dirs) are local; never commit binaries here.
- Local-only — never commit.
