# AGENTS.md — `.github/`

- Only `workflows/` (verified 2026-08-29); verify job details in the workflow
  files before citing.
- Project has its own `.git` — never stage/push from doc passes.
- Local-only under `projects/ongoing/` — never commit.
