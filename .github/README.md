# .github/

GitHub configuration for the steganographer repository: CI workflows under
`workflows/` (cargo build/test matrix), with repo hygiene config
(`.gitleaks.toml`, `deny.toml`, `release.toml`) at the project root.
