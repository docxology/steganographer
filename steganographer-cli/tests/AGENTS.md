# AGENTS.md — `steganographer-cli/tests/`

- Single file `cli_integration_tests.rs` (verified 2026-08-29); tallied as
  6 unit + 31 integration tests in root AGENTS.md.
- Integration tests exercise the real binary — no mocks.
- Local-only — never commit.
