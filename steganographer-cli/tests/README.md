# tests/

CLI integration tests: `cli_integration_tests.rs` — exercises the
`steganographer-cli` binary's 14 Clap subcommands end-to-end. Run:
`cargo test -p steganographer-cli` (or `cargo test --workspace`).
